<rs:TSDPopUp  xmlns:rs="clr-namespace:eThanhTra.Resource;assembly=eThanhTra.Resource"
                     x:Class="eThanhTra.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:eThanhTra" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" 
              
              mc:Ignorable="d"
        Title="ĐĂNG NHẬP" Height="450" Width="400" >

    <StackPanel Margin="40,80,40,80" FocusManager.FocusedElement="{Binding ElementName=txtUserName}">
        <rs:hTextEdit x:Name="txtUserName" CharacterCasing="Upper" Text="{Binding _Model.UserName}" _CaptionText="Tài khoản:" Margin="0,0,0,4"/>
        <rs:hPasswordBoxEdit Text="{Binding _Model.Password}" _CaptionText="Mật khẩu:" Margin="0,0,0,30"/>
        <dx:SimpleButton Glyph="{dx:DXImage Image=CheckBox_16x16.png}" Content="Đăng nhập" Height="50" Command="{Binding LoginCommand}"/>
    </StackPanel>
</rs:TSDPopUp>
